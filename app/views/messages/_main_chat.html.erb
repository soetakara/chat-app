 
    <div class="main_room">
      <div class="main_chatname">
      <%= @room.name %>
      </div>
      <div class="chat_end">
      <%= link_to "チャットを終了する", room_path(@room), method: :delete %>
      </div>
    </div>
    <div class="chat_center">
    <%= render partial: 'message', collection: @messages %>
      <%# <div class="chat">
        <div class="chat_top">
          <div class="chat_name">Taro</div>
          <div class="chat_date">2020/3/31(Wed) 12:43:30</div>
          </div>
        <div class="chat_text">おはよう</div>
      </div > %>
    </div>
    <%= form_with model: [@room, @message], class: 'chat_input', local: true do |f| %>
  <div class="form_input">
    <%= f.text_field :content, class: 'form_message', placeholder: 'type a message' %>
    <label class="form_image">
      <span class="image-file">画像</span>
      <%= f.file_field :image, class: 'hidden' %>
    </label>
  </div>
  <%= f.submit '送信', class: 'form_submit' %>
<% end %>
  </div>